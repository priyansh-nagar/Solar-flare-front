Fix these four specific issues in the dashboard. Do not change any visual styling, layout, or anything else — only fix these four problems:
Fix 1 — Add replay mode:

Add a replay control bar fixed at the bottom of the dashboard above the status bar. It should contain: a play/pause button (triangle/pause icon), a reset/restart button, a speed selector with options 1x, 5x, 10x, 20x (20x selected by default), and a date dropdown with these exact options: "2024-02-09 (X3.4)", "2024-02-22 (X6.4)", "2024-03-23 (X1.1)", "2024-05-06 (X4.5)", "2024-05-10 (X5.8)", "2024-10-03 (M5.1)". Also add a progress scrubber bar showing current window index out of total windows, with the current window number and total displayed in monospace to the right (e.g. "0234 / 1381"). When replay is active, call GET /replay?day=YYYYMMDD&window={currentIndex} every (1000/speed)ms, increment currentIndex each call, and update ALL dashboard values from the response — band_A_flux, band_C_flux, p_15min, p_30min, p_60min, p_extreme, goes_class, nowcast_active, timestamp, excess_A. When currentIndex reaches total windows, stop and show a "REPLAY COMPLETE" message. When nowcast_active is true in the response, flash the alert panel to ALERT state for 3 seconds then return to previous state, and add a row to the flare event database. Style the replay bar identically to the bottom status bar — same background #060A0E, same border-top, same monospace font. Play button: square, amber border and icon when active. Speed buttons: active state amber border and text. Scrubber: 2px track in #1E2D3D, filled portion in #4DAAFF, square thumb.
Fix 2 — Fix X-axis timestamps on light curve:

The X-axis currently shows repeated identical timestamps (23:11, 23:11, 23:11). This is because timestamps are not being correctly assigned to each data point in the rolling buffer. Fix the rolling 60-point data buffer so each point gets a unique timestamp — either from the API response timestamp field, or calculated as (currentTime - (60 - index) × 60 seconds) for each buffer position. The X-axis should show 6 evenly spaced time labels across the 60-minute window, each different, incrementing by 10 minutes. Format as HH:MM UTC.
Fix 3 — Fix MODEL and INFERENCE status indicators in bottom bar:

The bottom status bar shows "MODEL —" and "INFERENCE —" with dashes instead of real values. Fix these: MODEL indicator should show "LOADED" in green when the WebSocket is connected and receiving data. INFERENCE indicator should show the round-trip time in milliseconds between sending the /replay request and receiving the response — measure this with performance.now() before and after each fetch call and display as e.g. "142MS" in green. If WebSocket disconnects, both should revert to "—" in dim colour.
Fix 4 — Verify and fix flux value updates:

The soft X-ray (band_A_flux) and hard X-ray (band_C_flux) values displayed in the CURRENT CLASS hero panel must update every time a new /replay response arrives. Check that the component reading these values is subscribed to the same state that the API polling updates. If they are reading from a separate hardcoded variable or initial state that never updates, fix them to read from the live API response. Add a subtle flash animation (opacity 1 → 0.6 → 1, 0.3s) on the flux value text every time it updates, so it's visually clear the values are changing. Also fix the GOES class letter and SOFT/HARD class labels (currently showing "B-class", "A-class") to update from the goes_class field in the API response.

After all four fixes, test by: starting replay on 2024-02-22 at 20x speed and confirming that the flux values flash and change every second, the X-axis timestamps increment correctly, the MODEL shows LOADED and INFERENCE shows a millisecond value, and the alert fires when the X6.4 flare window hits.